// Cookie marker set by the API after a successful login. Used to decide whether
// to attempt a silent token refresh on startup and on 401 responses.
export const AUTH_COOKIE_MARKER = 'authenticated=true';
